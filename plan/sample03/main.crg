use std::fmt;
use std::vector::*;

fun main() {
    fold let v = vec()
    while let Ok(i) = fmt::readln().parse<u32>() {
        v.push(i)
    }
    fold let result = 0 for item in v.iter() {
        result + item
    }
    fmt::println("{}", result)
}
